# LLM provider — Gemini API key
API_LLM=your-gemini-api-key-here
LLM_MODEL=gemini-3.1-flash-lite

# Kokoro TTS (optional — falls back to ~/.config/kokoro-runtime/env)
KOKORO_MODEL=${HOME}/.local/share/kokoro/kokoro-v1.0.onnx
KOKORO_VOICES=${HOME}/.local/share/kokoro/voices-v1.0.bin
KOKORO_VOICE_DEFAULT=am_fenrir
KOKORO_LANGUAGE=es
KOKORO_SPEED=1.0

# Audio devices (use the index from the startup log, -1 = default)
# PROTOS_INPUT_DEVICE=-1
# PROTOS_OUTPUT_DEVICE=-1

# VAD sensitivity (lower = more sensitive, default 0.15)
# PROTOS_VAD_THRESHOLD=0.15

# Max recording time per turn in seconds (default 10)
# PROTOS_MAX_RECORD_SEC=10
